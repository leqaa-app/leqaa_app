import 'package:flutter/material.dart';

class FacultyInfoView extends StatelessWidget {
  const FacultyInfoView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}